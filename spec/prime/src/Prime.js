var Prime = {};
Prime.isPrime = function(inputNumber) {
    /* body... */
};
Prime.getPrime = function(inputNumber) {
	var result=[];
    if (inputNumber === 1 || inputNumber === 2) {
    	result.push(inputNumber);
        return result;
    }
   //  var canBeBreakdown = true;
   // while(inputNumber/2 === 0)
   // {

   // }
};

module.exports = Prime;
